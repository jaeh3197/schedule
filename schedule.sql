-- schedule 테이블 생성
CREATE TABLE `schedule` (
                            `ID`	LONG	NOT NULL,
                            `NAME`	STRING	NULL,
                            `PASSWORD`	lONG	NULL,
                            `TITLE`	STRING	NULL,
                            `CONTENT`	STRING	NULL,
                            `CREATED_AT`	DATE	NULL,
                            `MODIFIED_AT`	DATE	NULL
);

-- 